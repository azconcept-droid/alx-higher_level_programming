#!/usr/bin/env python3
if __name__ == "__main__":
    import sys
    length = len(sys.argv)
    if length == 1:
        print("0 arguments.")
    for i in range(length):

    length = '{}'.format(len(sys.argv))
